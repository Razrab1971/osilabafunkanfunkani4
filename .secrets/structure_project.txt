|
|- README.md / описание проекта для git
|- Dockerfile / сборка всего, того что пойдёт вместе с bot в один контейнер
|- docker-compose.yml / сборка всех контенеров + их настройки
|
|---- db / директория с файлами, для контенера с БД
	|- start-db.sh / файл отвечающий за начальную иницилизацию БД и последующие запуски
	|- start.sql   / начальные SQL скрипты (создание Таблиц, Пользователей, Триггеры + Заполнение)

|---- dop_application / Побочные консольные приложения (в основной на Cpp )
	| - shifr.cpp / Шифровальщик

|----  telegram_bot  / В основном все файлы для бота
	|- authenticate_db.py - файл с настройкой БД + создание(и получение) БД + методы с обёрткой SQL запросов( такой вот IPA для python) 
	|- view.py  / файл для создания менюшек и их шаблонов(menu_build - шаблон)
	|- main.py  / точка входа приложения( отождествлять с приложением )
	|- start.sh / скрипт для начала запуска контейнера
	|- requirements.txt / файлы для pip3 ( библиотеки python3 )
	|
	|---- buttons / Кнопки для главной менюшки + здесь уже реализация
		|- helloButton.py / Вызов up и down шапки у менюшки start в чате
		|- buttonsLLM.py  / Здесь обработчик вызоа LLM кнопки, а также те, что будут за AI, отвечать в этом разделе
	
	|---- commands / Всё то, что нужно нам как разработчикам( "Варп" ≈ непостижимый хаос )
		|- authenticate.py / Возможность менять имя в таблице БД
		|- prog_command.py / Панель разработчика + реализация этих самых кнопок ( это ЕДИНСТВЕННАЯ директория в которой, так можно - потому что разработчики произошли от матетематиков, а они, люди, очень ленивые )  
	
	|---- connect-AI / директория для задачи 4
	|---- script_bot / директория для задачи 6 

|---- .secrets / Папка для паролей, чтобы в контейнерах их не хранить напрямую
	|- db-password / Пароль от БД 
	|- structure_project / "Структура проекта"
